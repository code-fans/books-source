#include <bits/stdc++.h>
using namespace std;

int main()
{


  while(1)
  {
    //freopen("path.out","r",stdin);
    //freopen("path.out","w",stdout);
    int n;
    if(cin>>n)
      fclose(stdin);
    fclose(stdout);
    if(n!=0)
      break;
    system("random.exe");
    for(int i=0; i<=100000; i++);
    system("path.exe");
  }
  return 0;
}

